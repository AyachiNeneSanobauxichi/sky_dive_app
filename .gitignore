# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# Environment (keep .env.example, ignore real env files with secrets)
.env
.env.*
!.env.example

# Claude Code local (personal) settings — keep shared .claude/settings.json tracked
.claude/settings.local.json

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# gen-l10n 生成的未翻译报告（构建产物，不入库）
l10n_untranslated.txt

# gen-l10n 生成的 AppLocalizations（构建产物，不入库）
#
# 为什么不入库：入库就要求「改 .arb 必须紧跟一次 gen-l10n 并把产物一起提交」，
# 任何一半的回滚 / 合并冲突都会让产物与 .arb 脱钩——表现是 .arb 里明明有 key，
# 代码里 `l10n.xxx` 却报「未定义」，而且 diff 看不出问题（产物"干净"得像没动过）。
# 真实发生过一次。改成本地生成后，唯一的真源就是 .arb。
# 代价：clone 后、或 `flutter analyze` 之前需先跑一次 `flutter gen-l10n`
#（`flutter run` / `flutter build` 因 pubspec 的 `generate: true` 会自动生成）。
lib/l10n/app_localizations.dart
lib/l10n/app_localizations_*.dart
