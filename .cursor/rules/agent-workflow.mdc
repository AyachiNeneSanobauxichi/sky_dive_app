---
description: Agent 文档驱动工作流（业务/infra 边界、mock 生命周期、TODO 标记、_page/_api 拆分）
globs: agent/**/*.md,lib/features/**/*.dart
alwaysApply: false
---

# 16 · Agent 文档驱动工作流

> 本仓库用 `agent/` 目录下的 Markdown 文档驱动代码生成。本规则规定：文档如何组织、生成代码时的**业务 / infra 边界**、无接口时的 **mock 生命周期**、以及不确定处的 **TODO 标记**。这些是 agent 生成代码时的硬约束。

## 📁 文档组织：页面文档与接口文档分离

业务模块（`agent/service/<feature>/`）的文档**必须按职责拆分**成两份：

| 文档 | 角色 | 写什么 |
| --- | --- | --- |
| `<feature>.md` | **页面文档** | 只写**页面 UI 与交互逻辑**（布局、组件、状态流转、表单校验、导航意图）。不写接口契约 |
| `<feature>.api.md` | **接口文档** | 只写**接口与数据契约**（端点、请求/响应字段、DTO、错误码、缓存策略）。不写 UI |

例：`agent/service/auth/auth.md` + `agent/service/auth/auth.api.md`。

infra 文档（`agent/infra/...`，如 `base.md`、`route.md`）**只写 infra**，不掺业务页面细节。

> 一个文档一个职责：页面文档找 UI、接口文档找数据、infra 文档找基建。混写会让后续 agent 越界改动。

### 业务文档书写模式（必须遵守）

业务文档统一按下面的模式书写，agent 依此解析生成：

- **目录**：每个业务模块一个目录 `agent/service/<feature>/`，内含 `<feature>.md` 与 `<feature>.api.md` 两份文档。
- **版本化章节**：用 `## v1`、`## v2` … 递增章节记录每一期需求；**新增需求追加新版本章节，不改写历史版本**。每个版本下用无序列表逐条描述该期的需求 / 变更点。
- **`<feature>.md`（页面文档）**：标题 `# <feature>`；每个版本列出页面 UI 与交互需求（布局、组件、状态流转、表单校验、导航意图），**不写接口字段**。
- **`<feature>.api.md`（接口文档）**：标题 `# <feature> api`；每个接口一节，注明 method、path、鉴权要求，并用 TS 代码块给出 `request` / `response` 字段契约。

> infra 文档放 `agent/infra/`、学习笔记放 `agent/study/`，均不套用本业务模式。

### 项目定位文档：`agent/infra/base.md`（生成任何页面前先读）

`agent/infra/base.md` 记录**产品定位与全局取向**，是所有业务文档的上下文，同样按 `## vN` 版本化。当前（v1）确立的约束：

- 产品是**日本跳伞运营商的 C 端预约 app**（航线 / 预约 / 账号）；**UI / 交互是重中之重**，优先级高于"功能能跑通"。
- 视觉要现代且贴合本项目主题；**发现更优 UI / 交互方案必须主动提醒用户**（每次交付 UI 附 1–3 条建议）。
- 暂无后端，用 mock 把完整交互链路（含加载 / 空 / 错误态）跑通。

> 落地细则见 `ux-interaction.mdc`。读 `<feature>.md` 之前先把 base.md 当约束读一遍——页面文档只描述"要什么页面"，体验标准由 base.md + `ux-interaction.mdc` 兜。

## 🚧 生成边界：业务归业务，infra 不许碰

按**业务模块文档**（`<feature>.md` / `<feature>.api.md`）生成的代码，**只允许写业务层**——即 feature 内部：

```
lib/features/<feature>/{data,domain,controllers,screens,widgets}/
```

**禁止改动 infra**，包括但不限于：

- `lib/core/**`（network / DioClient / storage / error / config / providers …）
- `lib/app/**`（`router/`、路由表、`app.dart` 入口装配）
- `lib/core/theme/**`、全局 `lib/shared/widgets/**` 共享组件的既有 API
- `pubspec.yaml`、`l10n.yaml`、`analysis_options.yaml` 等工程配置

如果实现业务**确实**需要动 infra（例如缺一条路由、缺一个 provider、缺 i18n 基建），**不要擅自改**——**停下来重点询问用户，由人工操作**，并说明：需要动什么、为什么、影响面。

对应地，`agent/infra/*` 文档驱动的生成**只写 infra**，不写业务页面。

### 接口文档与页面文档各自的生成范围

同为业务层，但**两类文档生成的代码严格分工**：

| 文档 | 只允许生成 | 不得生成 |
| --- | --- | --- |
| `<feature>.api.md`（接口文档） | **接口定义相关**：`data/dto/`（DTO + JSON 序列化）、`data/<feature>_remote_data_source.dart`（端点）、`data/<feature>_repository.dart`（DTO↔Entity 映射、异常收敛），以及契约直接对应的 `domain/` 实体 | UI（`screens/`·`widgets/`）、`controllers/`、导航 |
| `<feature>.md`（页面文档） | **UI 与业务**：`screens/`、`widgets/`、`controllers/`（状态编排、交互、导航），复用接口文档已生成的 data/domain | 接口契约（DTO / 端点）——要改接口回 `<feature>.api.md` |

> 一句话：**接口文档 → 怎么拿数据；页面文档 → 怎么展示与交互**。缺了对应文档就停下询问，不要跨文档补写。
>
> 接口文档为空（契约未定）时不算"缺文档"：按下面的 mock 规范先把链路跑通，并把假设写成 `TODO(<scope>)`。

## 🧪 Mock 生命周期：无真实接口时

调用接口时若**尚无真实后端**：

1. 在**该 feature 的 data 同目录**下新建 `mock/` 文件夹，放 mock 数据与假数据源（如 `mock/<feature>_mock.dart`）。
2. 业务代码通过 mock 数据跑通 UI/逻辑；mock 只是临时占位。
3. **接入真实接口后，立即删除**不再需要的 `mock/` 文件夹与数据，切回真实 DataSource。

```
lib/features/<feature>/data/
  ├─ mock/                 # 无真实接口时临时存在，接完即删
  │   └─ <feature>_mock.dart
  ├─ <feature>_remote_data_source.dart
  └─ <feature>_repository.dart
```

> mock 文件夹是**临时资产**，不得长期留存。接真数据的 PR 里必须一并清掉。

## 📝 TODO 标记：不确定就留痕

任何**拿不准 / 待确认 / 临时方案**的地方，必须加带描述的 `TODO`，方便后续检索：

```dart
// TODO(auth): 后端未定登录返回结构，先按 {token, user} 假设，接口定了要改
// TODO(i18n): 文案待抽到 ARB，等 l10n 基建就绪
```

- 写清**为什么不确定 / 待办什么**，不要只写空 `// TODO`。
- 建议带模块前缀 `TODO(<scope>):`，便于全局搜索归类。

## 🌐 页面文案禁止硬编码（i18n）

页面（`screens/` `widgets/`）里**用户可见文案一律走国际化**，禁止出现中/英文字面量（见 `i18n.mdc`）。

> ⚠️ i18n 属于 infra：若 gen-l10n 基建（`l10n.yaml` + `lib/l10n/*.arb` + `AppLocalizations`）尚未就绪，业务 agent **不得自行搭建**，按上面「生成边界」停下询问，由人工先建基建。

## ✅ 自检清单（生成后）

- [ ] 只改了 feature 业务层，没碰 `core/` `app/` `theme/` 与工程配置。
- [ ] 交付 UI 的改动已过 `ux-interaction.mdc` 的交互自检清单（反馈四时刻、异步四态齐全、键盘、触感、无障碍）。
- [ ] 回复里附了「UI / 交互建议」1–3 条（改什么 / 为什么更好 / 代价），或明确说明当前实现已是更优方案。
- [ ] 依 `<feature>.api.md` 只生成了 data/domain 契约层，依 `<feature>.md` 只生成了 screens/widgets/controllers，无跨文档越界。
- [ ] 业务文档遵循 `<feature>.md` / `<feature>.api.md` 拆分 + `## vN` 版本化书写模式。
- [ ] 页面无硬编码文案（或已用 TODO 标出待抽取，且 i18n 基建缺失已上报）。
- [ ] 无真实接口的部分放在 `data/mock/`，并已说明「接完即删」。
- [ ] 所有不确定处都有带描述的 `TODO(<scope>):`。
- [ ] 需要动 infra 的点已单独列出、显式询问，而非擅自修改。
